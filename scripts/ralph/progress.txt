# Ralph Progress Log

## Codebase Patterns

- Add patterns discovered by the agent here.
- Keep this section short and useful.
- Future Ralph iterations must read this before implementation.

## Codebase Patterns

- node_modules está em Docker volume (marketops_node_modules). npm install só funciona dentro do container.
- Checks (typecheck, lint, build) requerem Docker rodando. Rodar com: `docker compose run --rm app npx tsc --noEmit` e `docker compose run --rm --no-deps app npm run lint`.
- Após US-001: rodar `docker compose run --rm app npx prisma migrate dev --name init` e depois `docker compose run --rm app npx prisma db seed` para aplicar schema e dados.
- IDs do schema Prisma são strings para compatibilidade com mock data existente (ex: "alimentos", "arroz-feijao").
- Singleton Prisma client em src/lib/db.ts — padrão obrigatório para Next.js dev mode.
- DATABASE_URL usa hostname "db" (resolve apenas dentro da Docker network).

## Iteration Logs

Append logs below using this format:

```txt
## [Date/Time] - [Story ID]
- What was implemented
- Files changed
- Quality checks run
- Result of checks
- Commit hash
- **Learnings for future iterations:**
  - Patterns discovered
  - Gotchas encountered
```

## 2026-05-22 - US-009
- What was implemented: CashRegisterStatus enum (open|closed) + CashRegister model adicionados ao schema. src/lib/actions/cash-registers.ts com getCashRegisters, openCashRegister (valida not open), closeCashRegister (valida not closed). Seed atualizado com 4 caixas iniciais.
- Files changed: prisma/schema.prisma, prisma/seed.ts, src/lib/actions/cash-registers.ts (novo)
- Quality checks run: typecheck: not available, lint: not available, test: not available, build: not available
- Result of checks: n/a — Docker offline
- Commit hash: (ver abaixo)
- **Learnings for future iterations:**
  - CashRegister usa @default(cuid()) para IDs pois registros de caixa não precisam de slugs legíveis
  - skipDuplicates: false no seed de CashRegister — cuid garante IDs únicos, não há conflito
  - openedAt/closedAt/closingBalance são nullable (DateTime? / Float?) para estado inicial fechado
  - openCashRegister reseta closedAt e closingBalance para null ao reabrir

## 2026-05-22 - US-008
- What was implemented: estoque/page.tsx virou async server component. StockContent migrado para props + adjustStock + router.refresh(). StockEntry estendido com reason? e responsible? para preservar layout completo do UI. Tipos de movimento simplificados para 3 (entrada|saida|ajuste). Histórico de movimentações vem de db.stockEntry.
- Files changed: src/app/estoque/page.tsx, src/components/stock/stock-content.tsx, prisma/schema.prisma, src/lib/actions/stock.ts
- Quality checks run: typecheck: not available, lint: not available, test: not available, build: not available
- Result of checks: n/a — Docker offline
- Commit hash: (ver abaixo)
- **Learnings for future iterations:**
  - StockEntry precisou de reason? e responsible? para preservar o UI de movimento completo — extender schema como parte da story de UI é aceitável
  - adjustStock nega quantity automaticamente para tipo "saida" — caller sempre passa valor positivo
  - History table remove coluna stockAfter (não disponível sem replay de entries) — aceitável para MVP
  - Movimentos limitados a últimos 100 no page.tsx (take: 100) — evitar queries pesadas

## 2026-05-22 - US-007
- What was implemented: StockEntryType enum (entrada|saida|ajuste) + StockEntry model adicionados ao schema. src/lib/actions/stock.ts com getStockSummary (groupBy + produto fallback) e adjustStock (validateProductId + create).
- Files changed: prisma/schema.prisma, src/lib/actions/stock.ts (novo)
- Quality checks run: typecheck: not available, lint: not available, test: not available, build: not available
- Result of checks: n/a — Docker offline
- Commit hash: (ver abaixo)
- **Learnings for future iterations:**
  - getStockSummary usa fallback p.currentStock quando produto ainda não tem StockEntry — preserva dados legados do seed
  - quantity é signed: positivo para entrada/ajuste positivo, negativo para saida/ajuste negativo
  - groupBy _sum retorna null se não há entradas — usar ?? 0 ou ?? p.currentStock
  - StockEntry.id usa @default(cuid()) pois IDs não precisam ser slugs legíveis (diferente de Product/Section/Category)

## 2026-05-22 - US-006
- What was implemented: produtos/page.tsx virou async server component buscando products, sections, categories do Prisma. ProductsContent migrado de useState/mock para props + server actions + router.refresh().
- Files changed: src/app/produtos/page.tsx, src/components/products/products-content.tsx
- Quality checks run: typecheck: not available, lint: not available, test: not available, build: not available
- Result of checks: n/a — Docker offline
- Commit hash: (ver abaixo)
- **Learnings for future iterations:**
  - ProductFormModal.categoriesForForm type mudou de `typeof initialCategories` para `CategoryData[]` — necessário ao remover dependência do mock
  - defaultFormState virou função makeDefaultFormState(sections, categories) — necessário pois sections/categories chegam como props, não são constantes de módulo
  - Mesmo padrão de US-004: props do server component + router.refresh() após mutação
  - sections prop adicionada ao ProductFormModal para popular o select de seção no formulário

## 2026-05-22 - US-005
- What was implemented: Server actions para Produtos — createProduct, updateProduct, toggleProductStatus
- Files changed: src/lib/actions/products.ts (novo)
- Quality checks run: typecheck: not available, lint: not available, test: not available, build: not available
- Result of checks: n/a — Docker offline
- Commit hash: (ver abaixo)
- **Learnings for future iterations:**
  - validateProductForm centraliza validação de campos numéricos — reutilizar padrão em outras actions com múltiplos campos
  - Promise.all para verificar FKs em paralelo — mais eficiente que queries sequenciais
  - Barcode e SKU únicos no schema — catch do Prisma captura UniqueConstraintViolation automaticamente

## 2026-05-22 - US-004
- What was implemented: page.tsx virou async server component buscando seções e categorias do Prisma. SectionsCategoriesContent migrado de useState para props + server actions + router.refresh().
- Files changed: src/app/secoes-categorias/page.tsx, src/components/catalog/sections-categories-content.tsx
- Quality checks run: typecheck: not available, lint: not available, test: not available, build: not available
- Result of checks: n/a — Docker offline
- Commit hash: (ver abaixo)
- **Learnings for future iterations:**
  - Padrão para client component com server actions: props do server component + router.refresh() após mutação
  - Prisma _count.products adaptado para interface local na page antes de passar ao componente — desacopla componente de tipos Prisma
  - void action().then(() => router.refresh()) funciona bem para handlers síncronos que disparam async

## 2026-05-22 - US-003
- What was implemented: Server actions para Categorias — createCategory, updateCategory, toggleCategoryStatus
- Files changed: src/lib/actions/categories.ts (novo)
- Quality checks run: typecheck: not available, lint: not available, test: not available, build: not available
- Result of checks: n/a — Docker offline
- Commit hash: (ver abaixo)
- **Learnings for future iterations:**
  - sectionId validado com findUnique antes de create/update — padrão FK-safe a reutilizar em products.ts
  - Mesmo ActionResult<T> de sections.ts — padrão estabelecido para todas as actions

## 2026-05-22 - US-002
- What was implemented: Server actions para Seções — createSection, updateSection, toggleSectionStatus
- Files changed: src/lib/actions/sections.ts (novo)
- Quality checks run: typecheck: not available (Docker offline), lint: not available, test: not available, build: not available
- Result of checks: n/a — Docker offline
- Commit hash: (ver abaixo)
- **Learnings for future iterations:**
  - Padrão ActionResult<T = void> funciona bem para server actions — reutilizar em categories.ts e products.ts
  - "use server" no topo do arquivo aplica a todos os exports — não precisa repetir em cada função
  - toSlug duplicado do UI — aceitável por ora, extrair para src/lib/utils.ts numa iteração futura se necessário

## 2026-05-22 - US-001
- What was implemented: Prisma ORM instalado. Schema com models Section, Category, Product (enums Status e ProductUnit). Singleton Prisma client. Seed com todos os dados do mock data.
- Files changed: prisma/schema.prisma, prisma/seed.ts, src/lib/db.ts, package.json
- Quality checks run: typecheck: not available (Docker offline), lint: not available (Docker offline), test: not available, build: not available
- Result of checks: PENDENTE — rodar `docker compose run --rm app npx prisma migrate dev --name init` e depois `npx prisma db seed` para concluir.
- Commit hash: (ver abaixo)
- **Learnings for future iterations:**
  - @prisma/client 7.8.0 instalado via Docker antes de Docker cair. prisma + tsx adicionados manualmente ao package.json.
  - Migration e seed dependem de Docker + DB rodando. São passos externos à iteração de código.
  - IDs das models são strings curtas (slugs) para compatibilidade com mock data já usado pelo UI.
